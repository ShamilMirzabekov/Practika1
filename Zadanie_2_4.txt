#include <iostream>
#include<cmath>
using namespace std;

int main() {
double N, A;
int M;
cin >> N;
M = N ;
if (N > 0 && N == M) {//почему N>0, а не N>=0

A = N + 10;
for (N; N <= A; N++) {
cout << N << " ";
}
} else {
cout << " Error ";
}

return 0;
}
